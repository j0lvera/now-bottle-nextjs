import React from "react";
import Layout from "../components/layout";

const Home = () => (
  <Layout>
    <div className="hero">
      <h1 className="title">Welcome!</h1>
    </div>
  </Layout>
);

export default Home;
